// Standalone layout — no app header/nav
// This page is the contractor's social bio link, opened from Instagram/TikTok
export default function ProPageLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
